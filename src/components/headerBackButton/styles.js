import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  text: {
    color: "#99aab5",
    fontSize: 18,
    marginHorizontal: 16,
    paddingHorizontal: 4,
    textAlign: "center",
  },
});

export default styles;
